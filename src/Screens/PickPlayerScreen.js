import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const PickPlayerScreen = () => {
  return (
    <View>
      <Text>PickPlayerScreen</Text>
    </View>
  );
};

export default PickPlayerScreen;

const styles = StyleSheet.create({});
